version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    - SERVICE_SUFFIX # service is "Evaluator", SDK reads EvaluatorClient
    - RPC_RESPONSE_STANDARD_NAME # streamed element is FlagChange, not WatchResponse
breaking:
  use:
    - FILE
